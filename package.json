{
  "name": "todomon",
  "version": "1.0.0",
  "description": "Get things done. Watch it grow.",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "npm --prefix frontend run build",
    "cap:sync:ios": "npm run build && npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "keywords": [],
  "author": "",
  "private": true,
  "dependencies": {
    "@capacitor/core": "^7.6.5",
    "@capacitor/ios": "^7.6.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.5",
    "typescript": "^6.0.3"
  }
}
